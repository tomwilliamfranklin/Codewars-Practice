public class TernaryOperator {

    public static boolean TernaryOperator(int i) {
     /*
     return_value = (true-false condition)
             ? (if true expression)
             : (if false expression);
      */
     return (i%2 == 0) ? true : false; //Notice how you don't need the "if"
    }
}
